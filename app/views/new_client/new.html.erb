<body>
  
  <h1>Nuevo cliente</h1>
    
    <p>Los campos marcados con un asterisco(*) son de llenado obligatorio.</p>
    
    <div class="centered">
      
    <%= simple_form_for :persona ,defaults: {wrapper: :inline_field2} do |f| %>
    
    

      <div class="form-group row row-centered">
        <%= f.input :idPersona, label: 'Id Empresa' %>
        <%= f.input :razonSocial, label: 'Razón social', as: :string%>
      </div>
        
      <div class="form-group row row-centered">
        <%= f.input :fechaCreac, label: 'Fecha de creación', 
          as: :date, start_year: Date.today.year,
          end_year: Date.today.year - 500,
          order: [:day, :month, :year] %>
          
      </div>
      
      <div class="form-group row row-centered">
        <%= f.input :pais, label: 'Ubicacióm', as: :string%>
        <%= f.input :provincia, label: 'Provincia', as: :string%>
        <%= f.input :ciudad, label: 'Ciudad', as: :string%>
      </div>
        
      <div class="form-group row row-centered"> 
        <%= f.input :dirExac, label: 'Dirección exacta', as: :string, wrapper: :inline_field6 %>
      </div>
        
      <div class="form-group row row-centered">
        <%= f.input :email, label: 'Correo de contacto', as: :string%>
      </div>
      
      <div class="form-group row row-centered">
        <%= f.button :submit, "Guardar cliente"%>
      </div>
      
    <% end %>
    
    </div>

</body>
